import { Quadra } from "./quadra";

export class Espaco{    
    id?: number =undefined;
    quadra: Quadra = new Quadra()
    nome: string = '';
    qtdMax?: number 
    descricao?: string = '';
    valor?: number
    
}